---
description: Commit staged changes (see .cursor/commands for the canonical workflow).
---

Runnable command: use **.cursor/commands/commit.md**. It defines how to write the commit message and run `git commit`.
